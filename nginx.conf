server {
    listen 80;

    resolver 127.0.0.11 valid=10s;

    location / {
        set $upstream http://web:5200;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
